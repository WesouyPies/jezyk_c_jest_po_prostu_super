#include<stdio.h>
 
void main ()
{
    int n;
    printf("Podaj liczbe\n");
    scanf("%d", &n);
 
    int i = 0;
    while (i < n ) {
        if (++i % 2 == 0) continue;
 
        printf("%d ", i);
    }
}
