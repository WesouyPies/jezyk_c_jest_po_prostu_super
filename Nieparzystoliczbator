#include<stdio.h>
 // Krzychu11 //
void main ()
{
    int n; // Ribut //
    printf("Podaj liczbe\n"); // Wyświetla liczbe //
    scanf("%d", &n);
 
    int i = 0;
    while (i < n ) {
        if (++i % 2 == 0) continue;
 
        printf("%d ", i);
    }
}
